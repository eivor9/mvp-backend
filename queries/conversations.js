const db = require("../db/dbConfig.js");

const getConversation = async () => {
  try {
    
  } catch (error) {
    return error;
  }
};

const createConversation = async () => {
    try {
  
    } catch (error) {
      return error;
    }
};

const deleteConversation = async () => {
  try {
    
  } catch (error) {
    return error;
  }
};

module.exports = { getConversation, createConversation, deleteConversation };